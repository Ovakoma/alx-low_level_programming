#include "main.h"
/**
 * _abs - prints the absolute value of an integer
 * @c: the value to be checked by the function
 * Description: prints absolute value of a number
 * Return: returns integer value 0
 */
int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	return (-c);
}
