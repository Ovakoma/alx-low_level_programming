#include "main.h"

/**
 * mul - multiplies two numbers and prints result
 * @a: one of the values to be multiplied
 * @b: other value for the product
 * Return: returns product result
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
